# --- Dati grezzi -----------------------------------------------------------
# I JSON di StatsBomb pesano gigabyte e si riscaricano con un comando.
# Una volta entrati nella cronologia git non si tolgono senza riscriverla.
data/raw/*
!data/raw/.gitkeep

# --- Dati trasformati: esclusi FINO A M7-T1 --------------------------------
#
# I Parquet di data/processed/ devono finire in git: sono cio' che Streamlit
# Cloud trova quando clona il repository, ed e' il criterio di M7-T1.
#
# Ma non adesso. Sono ancora un cantiere: si rigenerano a ogni passo — quando
# arrivano passes e touches, quando si scaricano le altre competizioni, quando
# M5 aggiunge l'xG calcolato. Git non sa fare diff dei binari, quindi ogni
# versione e' una copia intera: a regime sono ~4 MB l'uno, e otto rigenerazioni
# farebbero decine di megabyte di cronologia che nessuno potra' piu' togliere,
# perche' main e' protetto contro il force push di proposito.
#
# Si versionano quando sono il prodotto finito, non mentre sono in costruzione.
#
# >>> A M7-T1: cancellare le due righe qui sotto e committare i Parquet. <<<
data/processed/*.parquet

# --- Modelli addestrati: esclusi FINO A M7-T1 ------------------------------
#
# Stesso ragionamento dei Parquet — si rigenerano a ogni modifica delle
# variabili e git non sa fare diff dei binari — con un motivo in piu' che vale
# la pena scrivere.
#
# Un file `.pkl` e' Python serializzato: **caricarlo esegue codice**. Un
# modello nel repository e' quindi una superficie di attacco, e un modello
# committato per sbaglio da una macchina compromessa diventa codice che gira
# sul computer di chi clona. A M7-T1 ce ne sara' uno solo, prodotto da
# `scripts/train_model.py` e rigenerabile con un comando: quello e' il momento
# di versionarlo, non prima.
#
# >>> A M7-T1: cancellare la riga qui sotto e committare i modelli. <<<
models/*.pkl

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# --- Ambienti --------------------------------------------------------------
.venv/
venv/
env/
.python-version.local

# --- Strumenti -------------------------------------------------------------
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.hypothesis/

# --- Notebook --------------------------------------------------------------
.ipynb_checkpoints/

# --- Editor e sistema operativo -------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- Segreti ---------------------------------------------------------------
.env
.env.*
!.env.example
.streamlit/secrets.toml
